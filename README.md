# semantic file search
file search tool that uses embeddings to search files based on their semantic meanings instead of just contents or name \
Watches for new files in the home directory and creates embeddings for them which are stored in a chromadb.
